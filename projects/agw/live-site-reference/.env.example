# A.G. Williams Painting — environment template
# Keep this file free of live secrets.
# Only add `NEXT_PUBLIC_*` variables when a value is intentionally meant to ship to the browser.

SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Dashboard auth
DASHBOARD_PASSWORD=
DASHBOARD_JWT_SECRET=

# Optional integrations
GOOGLE_PLACES_SERVER_KEY=
FORMSPREE_ID=
